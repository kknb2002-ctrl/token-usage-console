# 회사에서 여러 PC로 쓰기 — 배포 가이드

## 0. 먼저 구분할 것: "앱"과 "데이터"

혼동하기 쉬운데 이 둘은 완전히 다른 문제입니다.

| | 앱(HTML·CSS·JS) | 데이터 |
|---|---|---|
| 무엇 | 화면을 그리는 파일 | 로그인 계정, API 키, 미팅·마케팅 내용 |
| 배포 | 정적 호스팅으로 충분 | **정적 호스팅만으로는 공유 불가** |

정적 호스팅(GitHub Pages 등)은 **파일을 나눠주는 곳**이지 데이터를 저장하는 곳이 아닙니다.
"각 PC에서 접속"은 이것만으로 되지만, "서로의 데이터를 실시간 공유"는 별도 수단이 필요합니다.

---

## 1. SaaS란 무엇인가

**Software as a Service** — 소프트웨어를 물건처럼 사서 설치하는 대신,
**남이 만들어 운영하는 웹 서비스에 매달 돈을 내고 쓰는 것**입니다.

| | 예전 방식 | SaaS |
|---|---|---|
| 구매 | CD 사서 PC에 설치 | 가입하고 주소로 접속 |
| 비용 | 한 번에 큰 금액 | 인당 월 몇 천 ~ 몇 만원 |
| 서버·백업·보안 | 우리가 관리 | **업체가 관리** |
| 업데이트 | 직접 설치 | 자동 |

이미 쓰고 계신 것들이 전부 SaaS입니다 — Notion, Slack, Google Workspace, Dropbox, 잔디, 더존.

**"SaaS를 산다"** = 이런 걸 직접 만들지 않고 **구독해서 쓴다**는 뜻입니다.
사람 5명이 쓸 도구를 만드는 데 개발 2주 + 서버 관리가 계속 든다면,
월 5만원짜리 SaaS를 쓰는 게 대부분 이깁니다.

### 그럼 이 ERP는 만들지 말아야 하나?

용도에 따라 다릅니다.

| 하고 싶은 일 | 이미 있는 SaaS | 직접 만들 가치 |
|---|---|---|
| 미팅 일정 공유 | 구글 캘린더, Notion | 낮음 — 그냥 쓰는 게 나음 |
| 마케팅 캠페인 관리 | Notion, Airtable | 낮음 |
| **AI 토큰 사용량을 한 화면에** | Langfuse, Helicone, OpenRouter 등 | **중간** — 회사 규칙에 맞추려면 직접이 나을 수 있음 |
| 우리 회사만의 업무 흐름 | 없음 | 높음 |

직접 만드는 이유는 보통 셋입니다 — ① 우리만의 규칙이 있다 ② 데이터를 밖에 두기 싫다 ③ 학습.
셋 다 아니라면 SaaS 구독이 정답인 경우가 많습니다.

---

## 2. 배포 방식 세 가지 — 고르는 게 아니라 단계

### ① 정적 호스팅 (GitHub Pages / Cloudflare Pages / Vercel)

```
개발자 → git push → 자동 배포 → https://erp.회사.com
```

- **비용** 무료 · **작업량** 30분 · **서버** 없음
- 로그인: 각자 PC의 브라우저에 저장 → **PC마다 따로 가입**
- 공용 데이터: `data/*.json` 을 커밋 → 배포되면 전원이 같은 값을 봄 (화면 수정은 불가)

### ② 도메인 + 백엔드

```
브라우저 → erp.회사.com → 서버(API) → 데이터베이스
```

- **비용** 도메인 1~2만원/년 + 호스팅 무료~월 3만원 · **작업량** 2~5일
- 로그인: 서버가 계정 관리 → **어느 PC에서 접속해도 같은 계정**
- 공용 데이터: DB에 저장 → **화면에서 바로 수정**, 전원에게 즉시 반영

> ⚠️ **도메인을 산다고 로그인이 공유되지 않습니다.** 도메인은 주소를 예쁘게 하는 것뿐이고,
> 핵심은 그 뒤의 **서버와 DB**입니다. 도메인은 ①에도 붙일 수 있습니다.

### ③ SSO (통합 로그인)

```
브라우저 → erp.회사.com → "회사 구글 계정으로 로그인" → Google이 신원 보증
```

- ERP 전용 비밀번호가 없어짐. 회사 계정 하나로 모든 사내 도구 로그인
- **퇴사자 계정을 정지하면 ERP 접근도 자동 차단** ← 이게 가장 큰 이유
- Google Workspace / MS 365를 이미 쓰면 추가 비용 거의 없음

---

## 3. 보통 회사는 어떻게 하나

| 규모 | 일반적인 선택 |
|---|---|
| 1~10명 | SaaS를 그냥 삽니다. 직접 만들면 ①로 시작 |
| 10~50명 | ② 클라우드 호스팅 + 관리형 DB, 로그인은 구글 SSO |
| 50명+ | ③ 사내 SSO(Google Workspace / Okta / Entra ID) 필수 + 권한·감사로그 |

**AI API 키는 방향이 갈립니다.**

- **개인 키 방식**(지금 구조): 각자 발급·각자 결제. 회사가 키를 안 가져 관리 부담이 적음
- **회사 키 방식**(규모가 커지면 대부분): 회사 조직 계정에 팀원 초대, 키는 서버에만.
  직원은 사내 게이트웨이로 호출하고 **개인이 키를 만질 일이 없어짐**

---

## 4. "우리 회사 사람만" 쓰게 하려면

지금 구조의 로그인은 **화면을 가리는 것**이지 접근을 막는 게 아닙니다.
주소를 아는 사람은 페이지 자체는 열 수 있고, 누구나 회원가입할 수 있습니다.
**진짜 차단은 호스팅 앞단에서 해야 합니다.**

### 추천: Cloudflare Pages + Cloudflare Access

요구사항 네 가지 — 주소만 주면 됨 / PC마다 로그인 유지 / 우리 회사만 / 비공개 저장소 —
를 **무료로** 한 번에 만족하는 조합입니다.

| 요구 | 해결 |
|---|---|
| 주소만 주면 각자 PC에서 | Cloudflare Pages가 URL 발급 (회사 도메인 연결 가능) |
| 비공개 저장소 유지 | **Private 저장소도 무료로 배포됨** (GitHub Pages와 다름) |
| 우리 회사 사람만 | Cloudflare Access — **회사 구글 계정 로그인 통과자만** 페이지에 도달 |
| 비용 | Access 무료 티어 **50명까지** |

Access를 켜면 URL이 유출돼도 회사 계정이 없으면 **로그인 화면조차 못 봅니다.**
사실상 ③ SSO를 서버 없이 얻는 셈입니다.

### 설정 순서

```bash
# 1) GitHub에 Private 저장소로 push
gh repo create erp-dashboard --private --source=. --push
```

2. **Cloudflare Pages** → Create project → GitHub 연결 → `erp-dashboard` 선택
   빌드 명령 없음 / 출력 폴더 `/` → Deploy (1분)
3. **Zero Trust → Access → Applications** → Add an application → Self-hosted
   도메인에 Pages 주소 입력
4. **Policy**: Action `Allow` / Include → `Emails ending in` → `@회사도메인.com`
5. 직원에게 주소만 전달 → 회사 구글 계정으로 인증하면 접속됨

> Vercel·Netlify도 Private 저장소를 무료로 배포합니다. 다만 "회사 계정만 접근"은
> Vercel의 경우 유료(Pro)라서, 무료로 하려면 Cloudflare Access가 가장 낫습니다.

---

## 5. 저장소 Public / Private 오해 정리

**저장소 공개 여부와 사이트 공개 여부는 다른 문제입니다.**

| 저장소 | 사이트 | 미팅·마케팅 공유 | 비고 |
|---|---|---|---|
| Public + GitHub Pages | 공개 | 됨 | ⚠️ 소스와 `data/*.json` 이 인터넷에 공개 |
| Private + GitHub Pages | — | — | GitHub **유료 플랜(Team 이상)** 필요 |
| **Private + Cloudflare Pages** | **공개 또는 사내 한정** | **됨** | **무료. 권장** |

즉 **Private으로 두면 공유할 수 없다는 것은 사실이 아닙니다.**
GitHub Pages만 유료를 요구할 뿐, Cloudflare Pages·Vercel·Netlify는 Private 저장소를 무료로 배포합니다.
소스는 감추고 사이트만 회사 사람에게 여는 게 정상적인 구성입니다.

---

## 6. 로그인 유지 (이미 적용됨)

로그인 화면의 **"이 PC에서 로그인 유지"** (기본 켜짐)

| 설정 | 저장 위치 | 브라우저 종료 후 |
|---|---|---|
| 켬 (기본) | localStorage | **14일간 로그인 유지**, 키도 바로 사용 가능 |
| 끔 | sessionStorage | 탭을 닫으면 로그아웃 + 키 잠김 |

사용 기록(호출 로그)도 PC에 저장되어 브라우저를 껐다 켜도 남습니다.

> ⚠️ 켜두면 **그 PC를 쓰는 사람은 브라우저를 다시 열어도 내 키를 쓸 수 있습니다.**
> 공용 PC나 회의실 PC에서는 반드시 끄고 쓰세요. 14일이 지나면 자동으로 다시 로그인해야 합니다.

---

## 7. 다음 단계 — 계정을 PC마다가 아니라 회사 단위로

①에서는 계정이 PC마다 따로입니다(회사 PC와 집 PC가 다른 계정).
그게 불편해지면 관리형 백엔드로 올라갑니다. 가장 적은 노력은 **Supabase**입니다.

| 필요 | Supabase로 해결 |
|---|---|
| 어느 PC에서든 같은 계정 | Auth (이메일 · 구글 SSO) |
| 미팅·마케팅을 화면에서 수정 | Postgres 테이블 |
| 개인 API 키 분리 | Row Level Security — 본인 행만 조회 |
| 비용 | 소규모 팀은 무료 티어로 충분 |

바꿀 코드는 세 군데뿐입니다 — `js/auth.js`(로그인), `js/shared.js`(공용 데이터 조회),
`js/datasource.js`의 `BACKEND`(사용량 수집).

---

## 8. 사이드바 메뉴 추가하는 법

`js/config.js` 의 `NAV` 배열 하나가 모든 페이지의 사이드바를 만듭니다. **한 줄만** 추가하면 됩니다.

```js
{ group: '공용', items: [
  { id: 'meetings', label: '미팅', icon: '📅', href: 'meetings.html', scope: 'shared' },
  { id: 'hr',       label: '인사', icon: '👥', href: 'hr.html',       scope: 'shared' }  // ← 추가
]}
```

- `scope: 'shared'` → 메뉴에 **공용** 배지, 데이터는 `data/*.json` 에서 읽음
- `scope: 'personal'` → 내 브라우저에만 있는 데이터
- 페이지는 `meetings.html` 을 복사해 `data-page` 와 `boot('...')` 인자만 바꾸면 됩니다
