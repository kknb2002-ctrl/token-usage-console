# 보안 정책

## 1. 저장소에는 API 키가 없습니다

소스 파일 어디에도 키가 들어 있지 않습니다. 키는 로그인 후 **계정 연결** 화면에서 각자 등록하며,
사용자의 브라우저에만 **암호문 상태로** 저장됩니다. `git push` 대상이 아니고 clone 한 사람에게도 전달되지 않습니다.

## 2. 로그인과 키 격리 (직원별)

| 항목 | 방식 |
|---|---|
| 비밀번호 | 저장하지 않음. PBKDF2-SHA256 · 210,000회로 만든 검증값만 보관 |
| 키 암호화 | 비밀번호에서 파생한 **별도의** AES-GCM 256비트 키로 암호화 |
| 복호화 키 | 로그인 유지 끔 → `sessionStorage`(탭 닫으면 잠김) / 켬 → `localStorage`(14일) |
| 저장 위치 | `erp.vault.<사용자id>.<제공자>` = `{iv, ct}` 암호문 |
| 화면 표시 | 마지막 4자리만 (`••••••••••••••••1234`), 원문은 DOM에 넣지 않음 |

비밀번호 검증용 salt와 금고 암호화용 salt를 분리해, 검증값이 유출돼도 금고 키를 역산할 수 없습니다.
**다른 직원이 자기 계정으로 로그인해도 남의 키는 복호화되지 않습니다** (`OperationError`).
비밀번호를 잊으면 키도 복구할 수 없으므로 재발급 후 다시 등록해야 합니다.

## 3. 이 방식의 한계 — 반드시 알고 쓰세요

브라우저만으로 만든 구조라 다음은 막지 못합니다.

- **내가 로그인한 상태로 자리를 비우면** 그 PC 앞의 사람은 내 화면을 쓸 수 있습니다.
- **"이 PC에서 로그인 유지"를 켜면** 브라우저를 껐다 켜도 로그인이 유지되므로,
  그 PC를 쓰는 사람은 언제든 내 키를 쓸 수 있습니다. 공용 PC에서는 반드시 끄세요.
- 개발자도구를 아는 사람이 **로그인된 탭에서** sessionStorage의 복호화 키를 꺼낼 수 있습니다.
- 사용자 목록이 브라우저에 있어, 그 PC에서 계정을 임의로 추가할 수 있습니다.

즉 **"같은 PC의 다른 로그인 사용자로부터의 격리"까지가 이 프로토타입의 보장 범위**입니다.
접근 자체를 회사 사람으로 제한하려면 호스팅 앞단에서 막아야 합니다
(Cloudflare Access 등 — [DEPLOY.md](DEPLOY.md) 4장).

## 4. 실서비스로 갈 때의 구조

```
[브라우저] --세션 쿠키--> [회사 서버] --키(서버 보관)--> [Anthropic / OpenAI / Google]
```

- 인증: 사내 SSO(Google Workspace, Okta 등) 또는 서버 세션
- 키 보관: 서버 환경변수 / KMS / Vault. **브라우저로 내려보내지 않음**
- 프런트엔드는 `js/datasource.js` 의 `BACKEND` 주소만 채우면 됩니다

브라우저에서 각 사 API를 직접 호출하지 마세요. 키가 노출되고 CORS로 차단됩니다.

## 5. 실수로 커밋하는 것을 막는 훅

`.githooks/pre-commit` 이 스테이징된 변경분에서 키 패턴(Anthropic/OpenAI/Google/GitHub/AWS)을 검사해
발견되면 커밋을 중단합니다. clone 한 사람은 아래 한 줄로 활성화합니다.

```bash
git config core.hooksPath .githooks
```

## 6. 키가 노출되었다면

각 사 콘솔에서 **즉시 폐기(revoke)** 하고 새로 발급하세요. 커밋 히스토리에 남았다면
파일을 지우는 것만으로는 부족합니다 — 키 폐기가 먼저입니다.
